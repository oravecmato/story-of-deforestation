// REMOVED (Pinia-unaware chart refactor): the chart-option classes are now instantiated directly
// inside each dumb chart component from typed props, and the injection bundle comes from
// `useChartContext()`. This file is dead — delete it (`rm app/charts/ChartOptionFactory.ts`).
export {}
